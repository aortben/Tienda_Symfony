{% extends 'base.html.twig' %}

{% block title %}Estado del Pedido - Tienda{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            
            <div class="card shadow-sm border-0">
                <div class="card-body p-5 text-center">

                    {# 
                        Aquí jugamos con la variable 'error' que nos pasa el controlador.
                        0 -> Todo perfecto, pedido realizado.
                        1 -> La cesta estaba vacía, ¡mal!
                        Otro -> Fallo técnico.
                    #}
                    {% if error == 0 %}
                        <h1 class="display-6 fw-bold text-success mb-3">¡Pedido Confirmado!</h1>
                        <p class="lead text-muted mb-4">Tu pedido ha sido procesado correctamente.</p>

                        <div class="bg-light rounded border p-3 mb-4">
                            <span class="text-muted small d-block text-uppercase ls-1">Número de Pedido</span>
                            <span class="display-6 fw-bold text-dark">#{{ pedido_id }}</span>
                        </div>

                        <p class="text-muted mb-4">
                            Hemos enviado un correo de confirmación a tu bandeja de entrada con el resumen de la compra.
                        </p>

                        <div class="d-grid gap-2">
                            <a href="{{ path('categorias') }}" class="btn btn-primary btn-lg">
                                Continuar Comprando
                            </a>
                            <a href="{{ path('historial') }}" class="btn btn-outline-secondary">
                                Ver mi historial
                            </a>
                        </div>

                    {% elseif error == 1 %}
                        {# CESTA VACÍA #}
                        <h2 class="fw-bold text-warning mb-3">Cesta Vacía</h2>
                        <p class="text-muted mb-4">
                            Tu cesta no contiene productos. Por favor, añade productos antes de procesar un pedido.
                        </p>
                        
                        <div class="d-grid">
                            <a href="{{ path('categorias') }}" class="btn btn-primary btn-lg">
                                Ir al Catálogo
                            </a>
                        </div>

                    {% else %}
                        {# ERROR #}
                        <h2 class="fw-bold text-danger mb-3">Error en el Pedido</h2>
                        <p class="text-muted mb-4">
                            Ocurrió un error técnico al procesar tu solicitud. Por favor, inténtalo de nuevo.
                        </p>
                        
                        <div class="d-grid">
                            <a href="{{ path('cesta') }}" class="btn btn-outline-secondary btn-lg">
                                Volver a la Cesta
                            </a>
                        </div>
                    {% endif %}

                </div>
            </div>

        </div>
    </div>
</div>
{% endblock %}